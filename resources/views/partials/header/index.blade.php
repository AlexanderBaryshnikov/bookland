<header class="site-header mo-left header style-1">
    <div class="header-info-bar">
        <div class="container clearfix">
            @include('partials.header.logo.index')
            @include('partials.header.user.index')
            @include('partials.search.index')
        </div>
    </div>

    <div class="sticky-header main-bar-wraper navbar-expand-lg">
        <div class="main-bar clearfix">
            <div class="container clearfix">
                @include('partials.header.logo.index')
                @include('partials.header.nav-toggle-btn.index')
                @include('partials.header.contact-us.index')
                @include('partials.header.menu.index')
            </div>
        </div>
    </div>
</header>

